# Abelian Sandpile Model Simulation

Реализация упрощённой модели Абеля. Утилита читает начальное состояние из TSV-файла, эволюционирует систему по правилам модели и сохраняет промежуточные состояния в виде изображений в формате BMP.

---

## 📋 Задача

|  |  |
|-------|-------------|
| Реализовать консольное приложение, которое:<br>- Читает начальное состояние из файла в формате **TSV** (tab-separated values).<br>- Эмулирует поведение **песчаной кучи** на двумерной сетке.<br>- Автоматически расширяет границы сетки при необходимости.<br>- Сохраняет состояние модели в виде **BMP-изображений** с заданной частотой.<br>- Работает **без использования сторонних библиотек** и контейнеров стандартной библиотеки C++ | ![merged (2)](https://github.com/user-attachments/assets/239af752-5020-4f00-8938-246e8dd5e914) |
---

## ⚙️ Аргументы командной строки

| Флаг | Полное имя | Описание |
|------|------------|----------|
| `-i` | `--input=`    | Путь к входному TSV-файлу с начальным состоянием |
| `-o` | `--output=`   | Путь к директории для сохранения BMP-изображений |
| `-m` | `--max-iter=` | Максимальное количество итераций модели. По умолчанию - до достижения стабильного состояния системы |
| `-f` | `--freq=`     | Частота сохранения изображений: каждые `N` итераций. Если `0` — сохраняется только финальное состояние. По умолчанию - 0 |
| `-h` | `--help` | Вывод справочной информации |

---

## 📂 Формат входного файла (TSV)

Каждая строка описывает одну ячейку в формате:
X Y Grains

Разделитель - символ табуляции Tab.

Где X, Y - координаты на поле `int16_t`

Grains - количество песчинок в ячейке `uint64_t`

---

## 🖼️ Формат выходных изображений (BMP)

Каждое сохранённое состояние записывается как **BMP-изображение по [BITMAPINFOHEADER](https://en.wikipedia.org/wiki/BMP_file_format)** со следующими правилами:

| Количество песчинок | Цвет пикселя | RGB (hex) |
|---------------------|--------------|-----------|
| 0                   | Белый        | `FFFFFF`  |
| 1                   | Зелёный      | `00FF00`  |
| 2                   | Фиолетовый   | `800080`  |
| 3                   | Жёлтый       | `FFFF00`  |
| ≥ 4                 | Чёрный       | `000000`  |

---

## 🔁 Сборка и запуск утилиты

Для сборки используется `CMake` от версии `3.20`

Пример запуска:
```
.\SandpileModel.exe --input=input.tsv -o output_directory --max-iter=1000 -f 1
```
